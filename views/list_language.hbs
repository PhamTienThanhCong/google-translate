<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>언어 목록</title>
</head>
<body>
    <style>
        /* form add langue */
        .form-add-langue{
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            margin: 0 auto;
            width: 50%;
        }
        .form-add-langue input{
            margin: 10px 0;
            width: calc(100% - 10px);
            outline: none;
            padding: 5px;
            font-size: 16px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }
        .form-add-langue button{
            margin: 10px 0;
        }
        /* form add langue */

        /* list langue view using table */
        .list-langue{
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            margin: 0 auto;
            width: 50%;
        }
        .list-langue table{
            border-collapse: collapse;
            width: 100%;
        }
        .list-langue th, .list-langue td{
            border: 1px solid #ddd;
            padding: 8px;
        }
        .list-langue tr:nth-child(even){background-color: #f2f2f2;}
        .list-langue tr:hover {background-color: #ddd;}
        .list-langue th {
            padding-top: 12px;
            padding-bottom: 12px;
            text-align: left;
            background-color: #4CAF50;
            color: white;
        }
        /* list langue view using table */
        .btn-back{
            padding: 0.5rem 1rem;
            font-size: 16px;
            background-color: rgb(87, 168, 245);
            color: rgb(0, 0, 0);
            border: none;
            border-radius: 5px;
            cursor: pointer;
            margin-top: 20px;
        }
    </style>
</body>
    <a href="/all-list">
        <button class="btn-back"><<< 사전 목록</button>
    </a>
    <form action="/create-langue-action" method="POST" class="form-add-langue">
        <h1>
            새로운 어학원 페이지
        </h1>
        <input type="text" name="name" placeholder="언어 이름">
        <input type="text" name="description" placeholder="설명하다">
        <input type="submit" value="언어 추가" style="width:100%; cursor:pointer">
    </form>

    <!-- list langue view -->
    <div class="list-langue">

        <table>
            <thead>
                <tr>
                    <th>언어 이름</th>
                    <th>설명하다</th>
                    <th>작업</th>
                </tr>
            </thead>
            <tbody>
                {{#each languages}}
                <tr>
                    <td>{{this.name}}</td>
                    <td>{{this.description}}</td>
                    <td>
                        <a href="/deleteLanguage/{{this._id}}">삭제</a>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
</html>